{
  "name": "muskelmekanikeren",
  "version": "1.0.0",
  "description": "A complete web app for Muskelmekanikeren",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "start": "node backend/server.js",
    "build": "cd frontend && npm run build && mv frontend/.output backend/.output"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.1",
    "nuxt": "^3.0.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-module": "^3.1.0"
  },
  "engines": {
    "node": ">=14.x",
    "npm": ">=6.x"
  }
}
